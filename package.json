{
  "name": "searchie-monorepo",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --cwd apps/searchie run dev",
    "build": "bun --cwd apps/searchie run build",
    "preview": "bun --cwd apps/searchie run preview",
    "tauri": "bun --cwd apps/searchie run tauri",
    "generate:settings-json": "bun --cwd apps/searchie run generate:settings-json",
    "test": "bun --cwd apps/searchie run test",
    "test:watch": "bun --cwd apps/searchie run test:watch",
    "test:coverage": "bun --cwd apps/searchie run test:coverage"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/core-plugins/*"
  ]
}
